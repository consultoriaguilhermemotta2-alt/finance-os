// ============================================================================
// SUPABASE — Browser client (placeholder)
// ----------------------------------------------------------------------------
// Ainda não utilizado. Quando a integração entrar:
//   1. npm install @supabase/ssr @supabase/supabase-js
//   2. Definir NEXT_PUBLIC_SUPABASE_URL e NEXT_PUBLIC_SUPABASE_ANON_KEY em
//      .env.local
//   3. Descomentar o código abaixo
//   4. Em lib/store.tsx, trocar os `useState(mockX)` por fetch ao Supabase
//      e cada `addX/updateX/deleteX` pela chamada equivalente
//      `.insert()/.update()/.delete()`, mantendo a mesma assinatura.
//
// Mapeamento de tabelas sugerido (ver lib/types.ts para os campos):
//   entradas, saidas, cartoes, compras_cartao (+ parcelas_cartao), dividas, metas
// ============================================================================

// import { createBrowserClient } from "@supabase/ssr";
//
// export function createClient() {
//   return createBrowserClient(
//     process.env.NEXT_PUBLIC_SUPABASE_URL!,
//     process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
//   );
// }

export {};
